services:
  orbelias-main:
    build:
      context: ../..
      dockerfile: examples/compose/Dockerfile
    command:
      - --id=0
      - --peers=0.0.0.0:7301
      - --data=/data/app-main
      - --galaxy=app-main
      - --user=app
      - --password=${ORBELIAS_APP_PASSWORD:-change-me}
      - --secret-key=${ORBELIAS_APP_SECRET_KEY:-change-me-too}
    ports:
      - "7301:7301"
    volumes:
      - orbelias_app_main:/data

volumes:
  orbelias_app_main:
