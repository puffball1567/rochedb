services:
  kouten-main:
    build:
      context: ../..
      dockerfile: examples/compose/Dockerfile
    command:
      - --id=0
      - --peers=0.0.0.0:7301
      - --data=/data/app-main
      - --galaxy=app-main
      - --user=app
      - --password=${KOUTEN_APP_PASSWORD:-change-me}
      - --secret-key=${KOUTEN_APP_SECRET_KEY:-change-me-too}
    ports:
      - "7301:7301"
    volumes:
      - kouten_app_main:/data

volumes:
  kouten_app_main:
