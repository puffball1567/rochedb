[package]
name = "rochedb"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Rust FFI driver for RocheDB"
repository = "https://github.com/rochedb/rochedb"
build = "build.rs"

[lib]
name = "rochedb"
path = "src/lib.rs"

[dependencies]
